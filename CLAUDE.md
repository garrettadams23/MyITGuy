# Git workflow conventions

- Default working/integration branch is `Testing`. Code changes should be
  committed and pushed directly to `Testing` automatically, without asking
  for confirmation each time.
- Never push to `main` or merge anything into `main` unless the user
  explicitly asks for it in that session.
